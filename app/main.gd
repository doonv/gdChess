extends Control


# Called when the node enters the scene tree for the first time.
func _ready():
#	var interface: UCIInterface = UCIInterface.new()
#	interface.send("uci")
#	for line in interface.read("uciok", 1000):
#	interface.send("ucinewgame")
#		print(line)
#	interface.send("position startpos")
#	interface.send("go depth 10")
#	for line in interface.read("bestmove", 1000):
#		print(line)
	pass

## Called every frame. 'delta' is the elapsed time since the previous frame.
#func _process(delta):
#	pass
